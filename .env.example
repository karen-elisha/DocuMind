# Weaviate Cloud — console.weaviate.cloud -> your sandbox -> Details
WEAVIATE_URL=https://your-cluster-id.weaviate.network
WEAVIATE_API_KEY=your-weaviate-admin-api-key
WEAVIATE_COLLECTION=DocumentNode

# Groq — console.groq.com -> API Keys (free, no card needed)
GROQ_API_KEY=your-groq-api-key
GROQ_VISION_MODEL=meta-llama/llama-4-scout-17b-16e-instruct

# Embedding model (local — BGE-small recommended for factual retrieval)
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5

# Chunking (character-based)
CHUNK_SIZE=800
CHUNK_OVERLAP=100

# Feature flags (dev-speed defaults: false)
ENABLE_VISION=false
ENABLE_EMBEDDINGS=false
ENABLE_WEAVIATE=false

# Docling pipeline controls
ENABLE_OCR=false
ENABLE_TABLE_STRUCTURE=true
