# Verified working together — LangChain 1.x line (major version bump from the
# old 0.x days, some import paths changed, hence the exact pins).

langchain==1.3.10
langchain-docling==2.0.0
langchain-huggingface==1.2.2
langchain-text-splitters==1.1.2
langchain-weaviate==0.0.7
weaviate-client==4.22.0
sentence-transformers==5.6.0
python-dotenv==1.2.2